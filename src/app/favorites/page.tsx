'use client'

import DashboardLayout from '../(dashboard)/layout'

export default function FavoritesPage() {
  return (
    <DashboardLayout>
      <section className="p-4">
        <h1 className="text-2xl font-bold">Your Favorite Prompts</h1>
        {/* TODO: Add favorites prompts grid */}
      </section>
    </DashboardLayout>
  );
}
